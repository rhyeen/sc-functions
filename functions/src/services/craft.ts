import { Game } from '@shardedcards/sc-types/node/game/entities/game.js';

export class CraftService {
    static getCraftingSupplies(game: Game): any {
        return { test: "hello world" };
    }
}